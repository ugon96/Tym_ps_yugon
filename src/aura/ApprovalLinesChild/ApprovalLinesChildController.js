/**
 * Created by Taejun.Kang on 2023-10-31.
 */

({
	fnInit : function(component, event, helper) {

		// var mapVal= component.get("v.mapValue");
		// var label = component.get("v.label");

		// var temp = Object.keys(mapVal);

		// for(var i in Object.keys(mapVal)){
		// 	// console.log(mapVal[temp[i]][0]);
		// 	// 리턴값 뒤에 3개는 approvalLine1-4 후 뒤의 값임..
		// 	if((temp.length-7) == i) break;

		// 	if(label == mapVal[temp[i]][0]){
		// 		var listApproval = mapVal[temp[i]];
		// 		listApproval.splice(0,2);
		// 		component.set("v.listApproval", listApproval);
		// 	}

		// }

 	}
})